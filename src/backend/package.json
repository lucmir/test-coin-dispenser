{
  "name": "test-coin-dispenser-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "jest --runInBand",
    "test-unit": "jest test/unit",
    "test-integration": "jest --runInBand test/integration",
    "test-coverage": "jest --runInBand --coverage test",
    "lint": "eslint services routes test --ext js",
    "lint-fix": "eslint services routes test --ext js --fix"
  },
  "dependencies": {
    "bitgo": "^4.37.1",
    "body-parser": "~1.13.2",
    "config": "^2.0.1",
    "cookie-parser": "~1.3.5",
    "dotenv": "^6.0.0",
    "express": "~4.13.1"
  },
  "devDependencies": {
    "eslint": "^5.6.1",
    "jest": "^23.6.0",
    "supertest": "^3.3.0"
  }
}
